import { Button } from "@xneek/ui-kit";

export default function Root(props) {
  return <section style={{ display: "flex", flexDirection: 'column', gap: 16 }}>
    APP-2 MICROFRONTEND CONTENT 2️⃣
  </section>;
}
